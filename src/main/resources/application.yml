spring:
  application:
    name: usersec
  datasource:
    url: jdbc:postgresql://localhost:5435/security
    username: postgres
    password: password
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true